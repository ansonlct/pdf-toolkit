# Validation — PDF Toolkit Mobile v1.2.0

Build checks performed:

- PASS: `node --check app.js`
- PASS: `node --check sw.js`
- PASS: `manifest.webmanifest` JSON parse
- PASS: GitHub Pages workflow retained
- PASS: no `.tool > span` description is generated by `renderTools()`
- PASS: dialog header no longer contains `dialogDesc`
- PASS: merge implementation contains first-page thumbnail rendering and Sortable drag ordering
- PASS: split implementation uses `VISUAL_SPLIT_THRESHOLD=20`, visual split markers, range mode and every-N helper
- PASS: protect implementation calls `PDFEncrypt.encryptPDF(..., {algorithm:'AES-256', ...})`

Runtime note:

A full headless-browser regression was not executed in this build container because the Playwright browser binary is not installed. The project therefore has static/syntax validation, not a claim of complete iPhone/Android end-to-end validation. Test on GitHub Pages HTTPS before treating the release as production-sealed.
